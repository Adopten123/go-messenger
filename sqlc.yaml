version: "2"
sql:
  - schema: "schema/migrations"
    queries: "schema/queries"
    engine: "postgresql"
    gen:
      go:
        package: "pgdb"
        out: "internal/repo/pgdb"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true